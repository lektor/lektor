// We actually re-export the same component as we use in the main admin here.
import BaseComponent from '../../../../lektor/admin/static/js/components/BaseComponent'

let Component = BaseComponent

export default Component
